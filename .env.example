# ThriveX 博客系统 - 环境变量配置模板
# =====================================================
# 说明：这是环境变量配置模板，请复制为 .env 文件并填入真实值

# 应用基础配置
JAVA_OPTS=-Xms256m -Xmx512m -Djava.security.egd=file:/dev/./urandom
SPRING_PROFILES_ACTIVE=pro
SERVER_PORT=8080

# 数据库连接配置
DB_HOST=your_database_host
DB_PORT=3306
DB_NAME=your_database_name
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# 邮件服务配置
EMAIL_HOST=smtp.qq.com
EMAIL_PORT=587
EMAIL_USERNAME=your_email@qq.com
EMAIL_PASSWORD=your_email_password
EMAIL_PROTOCOL=smtp
EMAIL_DEFAULT_ENCODING=UTF-8

# Spring Boot 数据源配置
SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver

# 邮件详细配置
SPRING_MAIL_HOST=${EMAIL_HOST}
SPRING_MAIL_PORT=${EMAIL_PORT}
SPRING_MAIL_USERNAME=${EMAIL_USERNAME}
SPRING_MAIL_PASSWORD=${EMAIL_PASSWORD}
SPRING_MAIL_PROTOCOL=${EMAIL_PROTOCOL}
SPRING_MAIL_DEFAULT_ENCODING=${EMAIL_DEFAULT_ENCODING}

# =====================================================
# 部署说明：
# 1. 复制此文件为 .env 并填入真实配置值
# 2. 在 Render 控制台中添加这些环境变量
# 3. 确保数据库表已通过 phpMyAdmin 导入 ThriveX.sql
# =====================================================